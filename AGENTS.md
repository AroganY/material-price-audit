# AGENTS.md — 材料询价（界面引导，少命令）

> **用户不通过命令行操作。**
> Agent 启动向导界面 → 用户在浏览器点选完成 → 你只在关键节点协助（登录确认、缺文件提示）。

---

## 0. 用户一说「询价 / 核价 / 跑工具」你就做

```bash
cd <package-root>
python3 -m material_price_audit serve
```

- 自动打开：`http://127.0.0.1:8765/`
- **对用户只说界面步骤**，不要让他敲 `run --platforms ...`
- 界面流程：
  1. 勾选平台（顺序=优先级）
  2. **选择每条材料询几个价**（默认 3，可改）
  3. 上传/选择询价表（表头任意）
  4. 识表确认
  5. 开始询价 → 浏览器登录会员站 → 点「我已登录继续」
  6. 下载结果

### 登录

程序弹出浏览器后，用户登录并回到向导点击「本站已登录，校验」。仅当后台询价已进入兼容的手工登录等待、且用户明确说「登完了」时，Agent 才代发继续信号：

```bash
touch data/output/LOGIN_CONTINUE
```

不要固定 sleep 90 秒刷新登录页。

---

## 1. 业务规则（必须对齐）

| 规则 | 行为 |
|------|------|
| 名称 + 规格 | **必须完全匹配** 才收价 |
| 当前平台无匹配 | **自动换下一勾选平台** |
| 所有勾选平台都无 | 该行 **留空**，状态 **「没查到」**，不编价 |
| 每条询几个价 | 用户在界面设置 K（1～10） |
| 表头 | 不固定，向导里识表 |

实现：`matching.strict_name_spec_match` + `inquiry.collect_quotes_for_item`。

---

## 2. 只在这些情况停下来问用户

1. 缺 Python/依赖 → `check --auto-install`，仍失败给安装提示
2. 没有询价表 → 让用户在界面上传，或把 xlsx 放进 `data/input/`
3. 未勾选平台 → 提醒在界面勾选
4. 登录 → 引导在浏览器登录，确认后继续

**禁止**逐步追问路径/是否试跑/是否全量；界面里用户自己选「试跑条数」。

---

## 3. 对用户话术

### 开场

```text
我帮你打开询价向导。浏览器打开后按页面步骤：
① 勾选要查的网站（广材/慧讯/领材…）
② 设置每条材料询几个价（比如 3）
③ 上传询价表
④ 点开始，在弹出窗口登录会员站，再点「我已登录继续」
匹配规则是名称和规格都要完全对上；对不上就换站，都没有就留空写「没查到」。
```

### 结束

```text
完成了。请在向导最后一页下载「结果 Excel」。
「没查到」的行在 RFQ 里，可发给供应商。
```

---

## 4. 路径

| 用途 | 路径 |
|------|------|
| 向导 | `http://127.0.0.1:8765/`（`serve`） |
| 询价表 | `data/input/*.xlsx` |
| 结果 | `data/output/result.xlsx` → 表「询价比价结果」 |
| 未查到 | `data/output/rfq.xlsx` |
| 证据 | `data/output/evidence.json` |
| 用户设置 | `data/user/settings.json` |

---

## 5. 禁止

- 禁止让用户主流程敲一长串 CLI
- 禁止无完全匹配就填价
- 禁止编造价格
- 禁止默认全站登录（只登录用户勾选的）
- 禁止提交 `.browser-profile` 与真实报价表

---

## 6. 可选（调试用，不对用户主推）

```bash
python3 -m material_price_audit parse --no-llm   # 仅识表
python3 -m material_price_audit check --auto-install
```
