# 材料询价核价工具 —— 给造价人员的说明

> 不用会编程。按下面做就行。  
> 目标只有一个：**把施工单位报的材料价，压到有依据、不超报送的合理水平。**

---

## 一、这个工具是干什么的？

### 你现在的痛点

| 现状 | 问题 |
|------|------|
| 施工单位报来一堆「报送不含税单价」 | 往往偏高 |
| 靠经验砍一刀 | 没依据，扯皮 |
| 自己上网查价 | 型号对不上、截图散乱、耗时间 |

### 用了之后

1. 你把 **询价表 Excel** 丢进去（表里要有「报送不含税单价」）。  
2. 你在浏览器里登录 **广材网 / 慧讯网 / 领材网**（或京东等，可选）。  
3. 工具自动按材料名称去查价。  
4. 出来一张结果表：
   - **审定不含税单价**（不高于报送价）  
   - **可点开的价格依据链接**（方便存档、对账）  
   - 查不到的材料 → 另出一张 **待询价表**，发给供应商盖章报价  

### 三条死规矩（保证审得住）

1. **查不到依据的，不填审定价**（宁缺毋滥，不瞎编）。  
2. **审定价绝不超过报送价**。  
3. 网上挂牌多是含税价，会自动按 **÷1.13** 粗算成不含税，再和报送价比低。  

> 输出是 **审核参考底稿**，最终仍以合同、品牌锁定、发票税率为准。

---

## 二、你需要准备什么？

### 1. 一张询价 Excel

表头尽量有这些列（名称接近即可）：

| 列 | 要不要 |
|----|--------|
| 材料名称 | 要 |
| 规格、型号 | 尽量有 |
| 单位、数量 | 尽量有 |
| **报送不含税单价** | **必须有** |
| 审定不含税单价 | 空着就行，工具填 |
| 品牌 | 有更好 |

### 2. 信息站账号（强烈建议）

广材 / 慧讯 / 领材 一般要 **会员登录** 才能看到价。  
请准备好你们单位已有的账号密码（自己登录，不要发给别人）。

### 3. 电脑上装一次环境（只会做一次）

若单位有信息化同事，把下面四行发给他即可：

```text
进入 material-price-audit 文件夹
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m playwright install chromium
```

装好后，终端执行 `python -m material_price_audit check`，显示 **OK** 就算成功。

---

## 三、操作流程（推荐：一键自动）

### 第 1 步：放询价表

把你的 Excel **原文件名**丢进：

```text
data/input/
```

例如 `安装专业询价材料设备.xlsx` 即可，**不必改名成 inquiry.xlsx**。  
工具会自动认含「报送不含税单价」的表。

（路径：`material-price-audit` → `data` → `input`）

### 第 2 步：选平台（可选，不选就用默认）

浏览器打开：

```text
docs/platform-select.html
```

勾选平台（顺序=优先级：先广材，匹配不上自动慧讯→领材→京东…）→ 点生成命令。

默认顺序：`广材网 → 慧讯网 → 领材网 → 京东 → 1688`  

| 平台 | 真实官网（登录会打开这里） |
|------|---------------------------|
| 广材网 | https://www.gldjc.com/login |
| 慧讯网 | https://services.iccchina.com/apply_trial |
| 领材网 | https://www.hylcw.cn/lcIndex.html |

三家**不是**同一个网站，会分别弹登录。

### 第 3 步：一键跑（或让 AI 跑）

```bash
cd material-price-audit
python -m material_price_audit run \
  --platforms guangcai,huixun,lingcai,jd,1688 \
  --auto-install \
  --login-wait 90
```

- 自动检查/安装环境  
- 弹出浏览器：你在约 90 秒内登录各网站（已登录可不管）  
- **自动瀑布匹配**：A 站详情规格型号对得上才用，否则自动换 B、C…  
- 自动生成结果 + 待询价表  

试跑 8 条（建议第一次）：

```bash
python -m material_price_audit run --platforms guangcai,huixun,lingcai --limit 8 --login-wait 90 --auto-install
```

### 第 4 步：打开结果

| 文件 | 干什么 |
|------|--------|
| `data/output/result.xlsx` | 审定结果，看「实抓汇总」，点开链接核对 |
| `data/output/rfq.xlsx` | 查不到的，发给供应商盖章 |

---

## 四、结果表怎么读？（造价视角）

打开 `result.xlsx`：

### 1）先看「实抓汇总」

- 每一行 = 查到了依据的一条材料  
- **审定不含税** = 建议核定价（≤ 报送）  
- **打开详情页** = 存档依据  

### 2）再看各专业原表

- 有审定价的：可直接作审核参考  
- 显示 pending / 待询价的：**没公开依据**，不要硬砍，走询价或认价流程  

### 3）和对方怎么说

> 「这条审定价依据见附件链接 / 截图，不高于贵司报送价；  
> 查不到公开价的，请按 rfq 表回盖章报价。」

---

## 五、不想碰命令行？让 AI 带着做

把下面整段复制给 Grok / Codex / Claude：

```text
请按 material-price-audit 的 AGENTS.md 带我做材料核价。
工作目录是 material-price-audit 文件夹。

请先执行：python -m material_price_audit init
然后按 AGENT_NEXT.md 一项一项问我，不要一次问完。
平台默认：广材网、慧讯网、领材网。
无证据不要填审定单价；先试跑 8 条再全表。
```

你只需要回答：

1. 询价表放好了吗？  
2. 登录完成了吗？  
3. 试跑结果型号对不对？  

---

## 六、常见问题（造价人版）

**Q：为什么很多材料没有审定价？**  
A：网上查不到同型号公开价（定制、人防专用等）。这是对的，应走供应商书面询价，不能瞎填。

**Q：审定价比市场零售还低/还高？**  
A：信息站价、工程经销价、零售价本来就不一样。链接点开核对；有疑问就改认价或补充询价。

**Q：必须上广材吗？**  
A：造价材料价优先广材/慧讯/领材。也可以加京东、1688 作对比，但品牌锁定工程要以合同约定为准。

**Q：会不会把我的账号传到网上？**  
A：登录在你自己电脑浏览器里完成；开源仓库也不会上传登录文件。不要把 `.browser-profile` 文件夹发给别人或传到 Git。

**Q：能代替信息价/定额吗？**  
A：不能。本工具解决的是 **认质认价 / 材料询价审核底稿**，与定额套用、信息价发布是不同环节。

---

## 七、一页纸流程（可打印）

```text
① 询价表（任意文件名）→ 放进 data/input/
② 登录广材 / 慧讯(iccchina) / 领材(hylcw)（浏览器）
③ 先跑 8 条 → 打开 result.xlsx 核对链接
④ 全表跑完 → 用 result 做审核
⑤ 查不到的 → rfq.xlsx 找供应商盖章
```

---

## 八、还要更细的？

| 你是谁 | 看哪个 |
|--------|--------|
| 造价人（你） | **本文** |
| 想网页点一点看 | `docs/index.html`（浏览器打开） |
| 信息中心 / 开发 | `docs/USAGE.md`、`README.md` |
| 让 AI 自动跑 | `AGENTS.md` |

有问题：把报错截图发给信息同事，或让 AI 按 `AGENTS.md` 排查。
